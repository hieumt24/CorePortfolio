# C# Coding Standards

- Use File-Scoped Namespaces
- Use PascalCase for public members (Classes, Methods, Properties)
- Use camelCase with `_` prefix for private fields
- Avoid Magic Strings - use constants
- Use explicit access modifiers
- Organize using directives: System namespaces first, followed by others alphabetically.
